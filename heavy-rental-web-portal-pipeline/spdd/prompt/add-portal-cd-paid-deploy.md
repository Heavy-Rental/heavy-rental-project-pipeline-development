# REASONS Canvas: add-portal-cd-paid-deploy

**R:** Paid portal CD via OIDC / `AWS_ACTUAL`. Academy unchanged. No Terraform.

**A:** Two callers, one reusable, `resolve-aws-profile`, SSM bucket on paid.

**S:** Paid YAML has no `aws_access_key_*`. Academy refuses non-academy.
